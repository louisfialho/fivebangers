<%= @user.email %>

<p>1st banger</p>
<% if @user.user_track_relationships.where(position: "1").empty? == false %>
  <%= link_to @user.user_track_relationships.where(position: "1").first.track.youtube_url, @user.user_track_relationships.where(position: "1").first.track.youtube_url, target: :_blank%>
<% end %>

<% if current_user == @user %>
  <%= form_for Track.new, :url => create_track_path(@track) do |f| %>
    <%= f.label :"update url" %>
    <%= f.text_field :youtube_url %>
    <%= hidden_field_tag 'position', '1'  %>
    <%= f.submit %>
  <% end %>
<% end %>

<p>2nd banger</p>
<% if @user.user_track_relationships.where(position: "2").empty? == false %>
    <%= link_to @user.user_track_relationships.where(position: "2").first.track.youtube_url, @user.user_track_relationships.where(position: "2").first.track.youtube_url, target: :_blank%>
<% end %>

<% if current_user == @user %>
  <%= form_for Track.new, :url => create_track_path(@track) do |f| %>
    <%= f.label :"update url" %>
    <%= f.text_field :youtube_url %>
    <%= hidden_field_tag 'position', '2'  %>
    <%= f.submit %>
  <% end %>
<% end %>

<p>3rd banger</p>
<% if @user.user_track_relationships.where(position: "3").empty? == false %>
    <%= link_to @user.user_track_relationships.where(position: "3").first.track.youtube_url, @user.user_track_relationships.where(position: "3").first.track.youtube_url, target: :_blank%>
<% end %>

<% if current_user == @user %>
  <%= form_for Track.new, :url => create_track_path(@track) do |f| %>
    <%= f.label :"update url" %>
    <%= f.text_field :youtube_url %>
    <%= hidden_field_tag 'position', '3'  %>
    <%= f.submit %>
  <% end %>
<% end %>

<p>4th banger</p>
<% if @user.user_track_relationships.where(position: "4").empty? == false %>
    <%= link_to @user.user_track_relationships.where(position: "4").first.track.youtube_url, @user.user_track_relationships.where(position: "4").first.track.youtube_url, target: :_blank%>
<% end %>

<% if current_user == @user %>
  <%= form_for Track.new, :url => create_track_path(@track) do |f| %>
    <%= f.label :"update url" %>
    <%= f.text_field :youtube_url %>
    <%= hidden_field_tag 'position', '4'  %>
    <%= f.submit %>
  <% end %>
<% end %>

<p>5th banger</p>
<% if @user.user_track_relationships.where(position: "5").empty? == false %>
    <%= link_to @user.user_track_relationships.where(position: "5").first.track.youtube_url, @user.user_track_relationships.where(position: "5").first.track.youtube_url, target: :_blank%>
<% end %>

<% if current_user == @user %>
  <%= form_for Track.new, :url => create_track_path(@track) do |f| %>
    <%= f.label :"update url" %>
    <%= f.text_field :youtube_url %>
    <%= hidden_field_tag 'position', '5'  %>
    <%= f.submit %>
  <% end %>
<% end %>

<p>Archivesss</p>

<% @user.user_track_relationships.where(position: "archived").order("updated_at DESC").each do |user_track_relationship| %>
  <p><%= user_track_relationship.track.youtube_url %></p> <br>
<% end %>

<% if current_user == @user %>
  <%= link_to "Log out", destroy_user_session_path, method: :delete %>
<% end %>

